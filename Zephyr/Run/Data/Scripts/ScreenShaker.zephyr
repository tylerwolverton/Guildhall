StateMachine
{
    String otherName;
    OnEvent( EnterTrigger )
    {
        //FireEvent( AddScreenShake( intensity = .07 ) );

        if( otherName == "player" )
        {
            FireEvent( SpawnEntity( type = "Blob" ) );
        }
    }

    OnEvent( StayTrigger )
    {
        //FireEvent( AddScreenShake( intensity = .005 ) );
    }

    OnEvent( ExitTrigger )
    {
        //FireEvent( AddScreenShake( intensity = .1 ) );
    }
}