StateMachine
{
	Number enemiesInLevel = 3;

	OnEvent( EnemyDied )
	{
		enemiesInLevel = enemiesInLevel - 1;

		FireEvent( UpdateEnemyCount( enemyCount = enemiesInLevel ) );
	}

	State Update
	{	
		//if( enemiesInLevel <= 0 )
		//{
		//	FireEvent( QuestComplete()  );
		//}
	}
}