StateMachine
{
	Number testNum = (-1 + 10) * 2;

	State TestState
	{	
		
		FireEvent( PrintDebugText( text = testNum )  );
		//FireEvent( PrintDebugText() );
		
		Number declarationTest; 
		Number testNum = (-1 + 10) * 2;


		testNum = 12 - 7;

		//declarationTest = testNum = variableTest = 4;
	}

	Number variableTest = 3;
}