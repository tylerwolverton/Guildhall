StateMachine
{
	// Event arguments to reference
	// TODO: Make this definition more intuitive
	String itemId;
	
	OnEvent( PickupItem )
	{
		if( itemId == "key1" )
		{
			FireEvent( DestroyEntity() );
		}
	}
}