StateMachine
{
	// Event arguments to reference
	// TODO: Make this definition more intuitive
	String itemId;
	
	OnEvent( PickupItem )
	{
		//FireEvent( PrintDebugText( text = itemId, duration = 5 ) );

		if( itemId == "key51" )
		{
			FireEvent( DestroyEntity() );
		}

		FireEvent( PrintDebugText( text = itemId, duration = 5 ) );
	}
}