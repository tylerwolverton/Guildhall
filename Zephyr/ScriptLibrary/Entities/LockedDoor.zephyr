ItemAcquired( item ) 
{
	if( item.name == this.keyName )
	{
		DestroySelf();
	}
}

CollisionEntered( other )
{
	if( other.name == "player" )
	{
		if( other.hasItem( this.keyName ) )
		{
			DestroySelf()
		}
	}
}
